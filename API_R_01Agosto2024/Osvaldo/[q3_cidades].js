import {get_number, print, get_text} from 'readline-sync'

function main{
    // Entrada
    const cidades = get_text ('Cidade: ')
    const rendapca = get_number ('Renda Per Capta: ')
    
while (rendapca > 0) {
    return cidades
}
    if (rendapca <=
        else if

        else

    // Processamento
    const total = cidades + rendapca
    const rendamedia = rendapca / total
    const menorrenda = rendapca <=  
    const maiorrenda = 


// Saida
print (`
Renda Média: R$${rendamedia}| Menor renda: R$${menorrenda}| Maior renda: R$${maiorrenda}
`)

}

main ()