import question from 'readline-sync'

function main(){
    const peso_atual = Number(question('Peso atual? '))
    const sexo = (question('Homem ou Mulher? '))
    const quilos_a_perder = Number(question('quilos a perder? '))
    const qts_dias_por_sem = Number(question('quantos dias por semana irá fazer atividade física? '))
    const tempo_por_dia = Number(question('Quanto tempo por dia irá treinar?'))
    const proporção = Number(question('qual a proporção de tempo alocada para o transport(%)'))

    const sem_obj_desejado = cal_sem_obj_desejado()
    const min_escada_dia = 



}
main()

function cal_sem_obj_desejado(){
    
} 